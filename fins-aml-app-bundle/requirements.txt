fastapi>=0.104.0
uvicorn[standard]>=0.24.0
databricks-sql-connector>=3.0.0
databricks-sdk>=0.20.0
databricks-vectorsearch>=0.40
openai>=1.0
httpx>=0.25.0
pydantic>=2.5.0
python-multipart>=0.0.6
aiofiles>=23.2.0
reportlab>=4.0.0
# psycopg is required only when USE_LAKEBASE=true (optional Postgres graph backend).
# Safe to leave installed even when the flag is off — the import is gated.
psycopg[binary]>=3.1.0